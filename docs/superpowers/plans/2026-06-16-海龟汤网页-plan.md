# 海龟汤网页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个可扩展的海龟汤网页应用：Flask 后端保护 Deepseek API 密钥，前端以单页应用形式提供故事选择、提问对话、线索解锁和汤底展示。

**Architecture:** Python Flask 提供三个 JSON API 并封装 Deepseek 调用；纯 HTML/CSS/JS 前端通过 Fetch 与后端交互；故事数据以独立 JSON 文件存放，新增故事只需复制模板。

**Tech Stack:** Python 3, Flask, Flask-CORS, python-dotenv, OpenAI Python SDK（Deepseek 兼容）, HTML/CSS/JS。

## File Structure

- `app.py`：Flask 后端，加载故事、暴露 API、调用 Deepseek
- `.env`：Deepseek API 密钥（不提交到 git）
- `.env.example`：环境变量示例
- `requirements.txt`：Python 依赖
- `stories/hiccup.json`：内置示例故事
- `static/index.html`：前端入口
- `static/css/style.css`：复古推理小说风格样式
- `static/js/app.js`：前端逻辑
- `tests/test_api.py`：后端 API 测试（Mock Deepseek）

## Task 1: 项目初始化与依赖

**Files:**
- Create: `requirements.txt`
- Create: `.env.example`
- Create: `.env`

- [ ] **Step 1: 创建 `requirements.txt`**

```text
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
openai>=1.30.0
pytest>=8.0.0
```

- [ ] **Step 2: 创建 `.env.example`**

```text
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

- [ ] **Step 3: 创建 `.env` 并填入真实密钥**

```text
DEEPSEEK_API_KEY=sk-wwczIH2EUnnJuNKjB2XIqMY9tgu0mwcmHoPfX3PUaYlNIJ04
```

> 注意：`.env` 已加入 `.gitignore`，不会提交。

- [ ] **Step 4: 安装依赖**

Run: `pip install -r requirements.txt`
Expected: 所有包安装成功，无报错。

## Task 2: 创建示例故事数据

**Files:**
- Create: `stories/hiccup.json`

- [ ] **Step 1: 创建目录并写入故事文件**

Run: `mkdir -p stories`

Create `stories/hiccup.json`:

```json
{
  "id": "hiccup",
  "title": "打嗝的男人",
  "summary": "一个男人走进酒馆要了一杯水，老板却掏枪对准他。男人说谢谢后离开。",
  "surface": "一个男人走进一家小酒馆，向老板要了一杯水。老板看了他一眼，突然从柜台下掏出一把手枪对准他。男人愣了一下，随即说“谢谢”，转身离开了酒馆。请问发生了什么？",
  "answer": "男人患有严重的打嗝，怎么都停不下来。他听说受到惊吓可以止住打嗝，于是故意走进酒馆要水喝，其实是希望老板能吓他一跳。老板看穿了他的把戏，掏出枪吓唬他。男人打嗝停了，所以道谢离开。",
  "core_clues": [
    "男人当时正在严重打嗝",
    "男人想要的是被惊吓，而不是水",
    "枪只是用来吓唬他，没有真正伤害他"
  ]
}
```

- [ ] **Step 2: 验证 JSON 格式**

Run: `python -c "import json; json.load(open('stories/hiccup.json', encoding='utf-8'))"`
Expected: 无报错退出。

## Task 3: 实现 Flask 后端基础

**Files:**
- Create: `app.py`

- [ ] **Step 1: 编写基础框架、故事加载和静态文件服务**

Create `app.py`:

```python
import os
import json
import re
from pathlib import Path

from dotenv import load_dotenv
from flask import Flask, request, jsonify, send_from_directory
from flask_cors import CORS
from openai import OpenAI

load_dotenv()

app = Flask(__name__, static_folder='static', static_url_path='')
CORS(app)

STORIES_DIR = Path(__file__).parent / 'stories'
VALID_ANSWERS = {'是', '否', '是也不是', '无关'}

client = OpenAI(
    api_key=os.environ.get('DEEPSEEK_API_KEY', ''),
    base_url='https://api.deepseek.com/v1',
)


def load_stories():
    stories = []
    if not STORIES_DIR.exists():
        return stories
    for path in sorted(STORIES_DIR.glob('*.json')):
        with open(path, 'r', encoding='utf-8') as f:
            stories.append(json.load(f))
    return stories


def get_story(story_id):
    for story in load_stories():
        if story.get('id') == story_id:
            return story
    return None


@app.route('/')
def index():
    return app.send_static_file('index.html')


@app.route('/api/stories')
def list_stories():
    stories = load_stories()
    return jsonify([
        {
            'id': s['id'],
            'title': s['title'],
            'summary': s.get('summary', s['surface'][:80] + '……'),
        }
        for s in stories
    ])


if __name__ == '__main__':
    app.run(debug=True, port=5000)
```

- [ ] **Step 2: 运行并测试故事列表接口**

Run: `python app.py`
In another terminal: `curl http://127.0.0.1:5000/api/stories`
Expected: 返回包含 `hiccup` 故事的 JSON 数组。

## Task 4: 实现 Deepseek 调用与回答规范化

**Files:**
- Modify: `app.py`

- [ ] **Step 1: 添加 Deepseek 调用函数和回答规范化**

Add to `app.py` before routes:

```python
def ask_deepseek(messages, model='deepseek-chat'):
    try:
        resp = client.chat.completions.create(
            model=model,
            messages=messages,
            temperature=0.3,
        )
        return resp.choices[0].message.content.strip()
    except Exception as e:
        raise RuntimeError(f'Deepseek API error: {e}')


def normalize_answer(text):
    text = text.strip().replace('。', '').replace('.', '')
    if text in VALID_ANSWERS:
        return text
    if '是也不是' in text:
        return '是也不是'
    if text.startswith('是'):
        return '是'
    if text.startswith('否') or text.startswith('不') or text.startswith('没'):
        return '否'
    return '无关'
```

- [ ] **Step 2: 实现 `/api/ask` 接口**

Add to `app.py` before `if __name__ == '__main__':

```python
@app.route('/api/ask', methods=['POST'])
def ask():
    data = request.get_json() or {}
    story_id = data.get('story_id')
    question = data.get('question', '').strip()
    history = data.get('messages', [])

    story = get_story(story_id)
    if not story:
        return jsonify({'error': 'Story not found'}), 404
    if not question:
        return jsonify({'error': 'Question is empty'}), 400
    if len(question) > 500:
        return jsonify({'error': 'Question too long'}), 400

    messages = [
        {
            'role': 'system',
            'content': (
                '你是海龟汤游戏的裁判。玩家只能看到汤面，正在通过提问推断真相。'
                '你只能回答以下四种之一：是、否、是也不是、无关。'
                '不要解释，只输出这四个字之一。\n\n'
                f'汤面：{story["surface"]}'
            ),
        },
    ]
    for msg in history:
        messages.append({'role': msg['role'], 'content': msg['content']})
    messages.append({'role': 'user', 'content': question})

    raw = ask_deepseek(messages)
    answer = normalize_answer(raw)
    return jsonify({'answer': answer, 'raw': raw})
```

- [ ] **Step 3: 手动测试提问接口**

With server running:
Run: `curl -X POST http://127.0.0.1:5000/api/ask -H 'Content-Type: application/json' -d '{"story_id":"hiccup","question":"男人是要水喝吗？","messages":[]}'`
Expected: 返回 `{"answer":"是"}` 或类似结果（需真实 API 密钥）。

## Task 5: 实现线索判定接口

**Files:**
- Modify: `app.py`

- [ ] **Step 1: 实现 `/api/check-clues` 接口**

Add to `app.py` before `if __name__ == '__main__':

```python
@app.route('/api/check-clues', methods=['POST'])
def check_clues():
    data = request.get_json() or {}
    story_id = data.get('story_id')
    history = data.get('messages', [])

    story = get_story(story_id)
    if not story:
        return jsonify({'error': 'Story not found'}), 404

    conversation = '\n'.join([
        f'{"玩家" if m["role"] == "user" else "裁判"}：{m["content"]}'
        for m in history
    ])

    prompt = (
        '你是海龟汤游戏的内部裁判，知道完整汤底。'
        '请根据玩家与裁判的对话，判断玩家是否已经掌握了以下核心线索。'
        '对每条线索，如果玩家通过推理明显掌握了该事实，返回 true；否则返回 false。'
        '只返回 JSON 数组，不要有任何解释。\n\n'
        f'汤底：{story["answer"]}\n\n'
        f'核心线索：{json.dumps(story["core_clues"], ensure_ascii=False)}\n\n'
        f'对话：\n{conversation}'
    )

    messages = [
        {'role': 'system', 'content': '你是一个严格的 JSON 输出工具。'},
        {'role': 'user', 'content': prompt},
    ]
    raw = ask_deepseek(messages)

    try:
        match = re.search(r'\[.*\]', raw, re.DOTALL)
        if match:
            result = json.loads(match.group(0))
        else:
            result = json.loads(raw)
        if not isinstance(result, list) or len(result) != len(story['core_clues']):
            result = [False] * len(story['core_clues'])
    except Exception:
        result = [False] * len(story['core_clues'])

    discovered = [i for i, v in enumerate(result) if v is True]
    return jsonify({'discovered_clues': discovered})
```

- [ ] **Step 2: 手动测试线索判定**

With server running:
Run: `curl -X POST http://127.0.0.1:5000/api/check-clues -H 'Content-Type: application/json' -d '{"story_id":"hiccup","messages":[{"role":"user","content":"男人当时是不是在打嗝？"},{"role":"assistant","content":"是。"}]}'`
Expected: 返回 `{"discovered_clues":[0]}` 或类似结果。

## Task 6: 实现前端 HTML

**Files:**
- Create: `static/index.html`

- [ ] **Step 1: 创建前端页面结构**

Create `static/index.html`:

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>海龟汤</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">海龟汤</div>
    </header>

    <main id="page-list" class="page">
      <section class="intro">
        <h1>选择一个故事开始推理</h1>
        <p>阅读汤面，通过提问逼近真相。AI 只会回答“是 / 否 / 是也不是 / 无关”。</p>
      </section>
      <section id="story-list" class="story-list"></section>
    </main>

    <main id="page-game" class="page" style="display:none;">
      <div class="game-header">
        <button id="back-btn" class="back-btn">返回故事列表</button>
        <h1 id="game-title"></h1>
      </div>
      <div class="game-body">
        <div class="left">
          <section class="surface">
            <div class="surface-label">汤面</div>
            <p id="surface-text"></p>
          </section>
          <section id="chat" class="chat"></section>
          <section class="input-area">
            <input id="question-input" type="text" placeholder="输入你的问题……" maxlength="500">
            <button id="send-btn">发送</button>
          </section>
          <div id="loading" class="loading" style="display:none;">侦探思考中……</div>
        </div>
        <aside class="right">
          <div class="clue-board">
            <h3>侦探笔记</h3>
            <p id="clue-status" class="note-status">调查刚刚开始，还没有发现关键信息……</p>
            <div id="clue-list"></div>
            <div id="answer-section" class="answer-section locked">
              <p>继续追问，更多真相会浮出水面。</p>
              <button disabled>揭开汤底</button>
            </div>
            <div id="answer-text" class="answer-text" style="display:none;"></div>
          </div>
        </aside>
      </div>
    </main>
  </div>
  <script src="/js/app.js"></script>
</body>
</html>
```

## Task 7: 实现前端样式

**Files:**
- Create: `static/css/style.css`

- [ ] **Step 1: 创建复古推理小说风格样式**

Create `static/css/style.css`:

```css
* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: "Noto Serif SC", Georgia, serif;
  background: #F5F0E6 url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.08'/%3E%3C/svg%3E");
  color: #3D2B1F;
  min-height: 100vh;
  padding: 24px;
}

.container {
  max-width: 1100px;
  margin: 0 auto;
  background: #FDFBF6;
  border: 1px solid #D9CFC0;
  box-shadow: 0 8px 32px rgba(61,43,31,0.15);
  min-height: calc(100vh - 48px);
  display: flex;
  flex-direction: column;
}

header {
  padding: 24px 32px;
  border-bottom: 2px solid #3D2B1F;
}

.logo {
  font-size: 28px;
  font-weight: 700;
  letter-spacing: 4px;
}

.page {
  flex: 1;
  padding: 32px;
}

.intro h1 {
  font-size: 24px;
  margin-bottom: 12px;
}

.intro p {
  color: #6B5E52;
  margin-bottom: 32px;
}

.story-list {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
}

.story-card {
  padding: 24px;
  background: #FDFBF6;
  border: 1px solid #D9CFC0;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}

.story-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 6px 20px rgba(61,43,31,0.12);
}

.story-card h3 {
  font-size: 18px;
  margin-bottom: 10px;
}

.story-card p {
  font-size: 14px;
  color: #6B5E52;
  line-height: 1.6;
}

.game-header {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 24px;
}

.back-btn {
  padding: 8px 16px;
  border: 1px solid #3D2B1F;
  background: transparent;
  color: #3D2B1F;
  font-family: inherit;
  cursor: pointer;
}

.game-header h1 {
  font-size: 22px;
}

.game-body {
  display: grid;
  grid-template-columns: 1fr 320px;
  gap: 0;
}

.left {
  border-right: 1px solid #D9CFC0;
  display: flex;
  flex-direction: column;
}

.surface {
  padding: 32px;
  background: linear-gradient(to bottom, #FDFBF6 0%, #F7F2E8 100%);
  border-bottom: 1px dashed #C4B8A8;
}

.surface-label {
  font-size: 12px;
  letter-spacing: 3px;
  color: #8B2635;
  margin-bottom: 12px;
}

.surface p {
  line-height: 1.9;
  font-size: 16px;
}

.chat {
  flex: 1;
  padding: 24px 32px;
  min-height: 240px;
  max-height: 400px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.message {
  max-width: 80%;
  padding: 14px 18px;
  line-height: 1.7;
}

.message.user {
  align-self: flex-end;
  background: #EBE4D6;
  border: 1px solid #D9CFC0;
}

.message.assistant {
  align-self: flex-start;
  background: #FDFBF6;
  border: 1px solid #C4B8A8;
  border-left: 3px solid #8B2635;
}

.input-area {
  padding: 20px 32px;
  border-top: 1px solid #D9CFC0;
  display: flex;
  gap: 12px;
  background: #F7F2E8;
}

.input-area input {
  flex: 1;
  padding: 12px 16px;
  border: 1px solid #A89B8C;
  background: #FDFBF6;
  font-family: inherit;
  font-size: 15px;
  outline: none;
}

.input-area button {
  padding: 12px 24px;
  background: #8B2635;
  color: #FDFBF6;
  border: none;
  font-family: inherit;
  cursor: pointer;
}

.input-area button:disabled {
  background: #A89B8C;
  cursor: not-allowed;
}

.loading {
  padding: 12px 32px;
  color: #6B5E52;
  font-style: italic;
}

.right {
  padding: 32px 24px;
  background: #F7F2E8;
}

.clue-board {
  position: sticky;
  top: 24px;
}

.clue-board h3 {
  font-size: 16px;
  margin-bottom: 20px;
  padding-bottom: 8px;
  border-bottom: 1px solid #C4B8A8;
}

.note-status {
  font-size: 13px;
  color: #6B5E52;
  margin-bottom: 16px;
  font-style: italic;
}

.clue {
  padding: 14px;
  margin-bottom: 12px;
  background: #FDFBF6;
  border: 1px solid #D9CFC0;
  font-size: 14px;
  line-height: 1.6;
  border-left: 3px solid #8B2635;
}

.answer-section {
  margin-top: 24px;
  padding: 20px;
  border: 2px solid #8B2635;
  background: #FDFBF6;
  text-align: center;
}

.answer-section.locked {
  opacity: 0.5;
  border-style: dashed;
}

.answer-section button {
  padding: 10px 20px;
  background: #8B2635;
  color: #FDFBF6;
  border: none;
  font-family: inherit;
  cursor: pointer;
}

.answer-section button:disabled {
  background: #A89B8C;
}

.answer-text {
  margin-top: 20px;
  padding: 20px;
  background: #FDFBF6;
  border: 1px solid #C4B8A8;
  line-height: 1.9;
  font-size: 15px;
}

@media (max-width: 768px) {
  body { padding: 12px; }
  .page { padding: 20px; }
  .game-body { grid-template-columns: 1fr; }
  .left { border-right: none; border-bottom: 1px solid #D9CFC0; }
  .chat { padding: 16px 20px; }
  .input-area { padding: 16px 20px; }
}
```

## Task 8: 实现前端逻辑

**Files:**
- Create: `static/js/app.js`

- [ ] **Step 1: 编写前端 JavaScript**

Create `static/js/app.js`:

```javascript
const API_BASE = '';

let stories = [];
let currentStory = null;
let messages = [];
let discoveredClues = new Set();

const pages = {
  list: document.getElementById('page-list'),
  game: document.getElementById('page-game'),
};

async function init() {
  try {
    const resp = await fetch(`${API_BASE}/api/stories`);
    stories = await resp.json();
    renderStoryList();
  } catch (err) {
    document.getElementById('story-list').innerHTML =
      '<p class="note-status">无法加载故事列表，请检查网络。</p>';
  }
}

function renderStoryList() {
  const container = document.getElementById('story-list');
  container.innerHTML = '';
  stories.forEach(story => {
    const card = document.createElement('div');
    card.className = 'story-card';
    card.innerHTML = `<h3>${escapeHtml(story.title)}</h3><p>${escapeHtml(story.summary)}</p>`;
    card.onclick = () => startGame(story.id);
    container.appendChild(card);
  });
}

function startGame(storyId) {
  currentStory = stories.find(s => s.id === storyId);
  if (!currentStory) return;
  messages = [];
  discoveredClues = new Set();
  document.getElementById('answer-text').style.display = 'none';
  document.getElementById('answer-text').textContent = '';
  renderGame();
  showPage('game');
}

function renderGame() {
  document.getElementById('game-title').textContent = currentStory.title;
  document.getElementById('surface-text').textContent = currentStory.surface;
  renderChat();
  renderClues();
}

function renderChat() {
  const chat = document.getElementById('chat');
  chat.innerHTML = '';
  messages.forEach(msg => {
    const div = document.createElement('div');
    div.className = `message ${msg.role}`;
    div.textContent = msg.content;
    chat.appendChild(div);
  });
  chat.scrollTop = chat.scrollHeight;
}

function renderClues() {
  const board = document.getElementById('clue-list');
  const status = document.getElementById('clue-status');
  const answerSection = document.getElementById('answer-section');
  board.innerHTML = '';

  if (discoveredClues.size === 0) {
    status.textContent = '调查刚刚开始，还没有发现关键信息……';
  } else {
    status.textContent = '你在调查中记下了这些发现：';
    Array.from(discoveredClues).sort().forEach(idx => {
      const clue = document.createElement('div');
      clue.className = 'clue';
      clue.textContent = currentStory.core_clues[idx];
      board.appendChild(clue);
    });
  }

  if (discoveredClues.size === currentStory.core_clues.length) {
    answerSection.classList.remove('locked');
    answerSection.innerHTML =
      '<p>所有关键信息都已串联起来，真相呼之欲出。</p>' +
      '<button id="reveal-answer">揭开汤底</button>';
    document.getElementById('reveal-answer').onclick = revealAnswer;
  } else {
    answerSection.classList.add('locked');
    answerSection.innerHTML =
      '<p>继续追问，更多真相会浮出水面。</p>' +
      '<button disabled>揭开汤底</button>';
  }
}

async function sendQuestion() {
  const input = document.getElementById('question-input');
  const question = input.value.trim();
  if (!question) return;
  if (question.length > 500) return;
  input.value = '';

  messages.push({ role: 'user', content: question });
  renderChat();
  setLoading(true);

  try {
    const askResp = await fetch(`${API_BASE}/api/ask`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        story_id: currentStory.id,
        messages: messages.slice(0, -1),
        question,
      }),
    });
    if (!askResp.ok) throw new Error('Ask failed');
    const askData = await askResp.json();
    messages.push({ role: 'assistant', content: askData.answer });
    renderChat();

    const clueResp = await fetch(`${API_BASE}/api/check-clues`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ story_id: currentStory.id, messages }),
    });
    if (!clueResp.ok) throw new Error('Check clues failed');
    const clueData = await clueResp.json();
    clueData.discovered_clues.forEach(i => discoveredClues.add(i));
    renderClues();
  } catch (err) {
    messages.push({ role: 'assistant', content: '侦探暂时无法回答，请稍后再试。' });
    renderChat();
  } finally {
    setLoading(false);
  }
}

function revealAnswer() {
  const answerDiv = document.getElementById('answer-text');
  answerDiv.textContent = currentStory.answer;
  answerDiv.style.display = 'block';
  answerDiv.scrollIntoView({ behavior: 'smooth' });
}

function showPage(name) {
  Object.values(pages).forEach(p => (p.style.display = 'none'));
  pages[name].style.display = 'block';
}

function setLoading(loading) {
  document.getElementById('send-btn').disabled = loading;
  document.getElementById('loading').style.display = loading ? 'block' : 'none';
}

function escapeHtml(text) {
  const div = document.createElement('div');
  div.textContent = text;
  return div.innerHTML;
}

document.getElementById('send-btn').onclick = sendQuestion;
document.getElementById('question-input').addEventListener('keypress', e => {
  if (e.key === 'Enter') sendQuestion();
});
document.getElementById('back-btn').onclick = () => showPage('list');

init();
```

## Task 9: 编写后端测试

**Files:**
- Create: `tests/test_api.py`

- [ ] **Step 1: 创建测试文件**

Run: `mkdir -p tests`

Create `tests/test_api.py`:

```python
import pytest
from unittest.mock import patch, MagicMock
from app import app


@pytest.fixture
def client():
    app.config['TESTING'] = True
    with app.test_client() as client:
        yield client


def test_index(client):
    resp = client.get('/')
    assert resp.status_code == 200
    assert b'海龟汤' in resp.data


def test_list_stories(client):
    resp = client.get('/api/stories')
    assert resp.status_code == 200
    data = resp.get_json()
    assert len(data) >= 1
    assert data[0]['id'] == 'hiccup'


@patch('app.client')
def test_ask_returns_normalized_answer(mock_openai, client):
    mock_openai.chat.completions.create.return_value = MagicMock(
        choices=[MagicMock(message=MagicMock(content='是的。'))]
    )
    resp = client.post('/api/ask', json={
        'story_id': 'hiccup',
        'question': '男人要的是水吗？',
        'messages': [],
    })
    assert resp.status_code == 200
    assert resp.get_json()['answer'] == '是'


@patch('app.client')
def test_ask_rejects_empty_question(mock_openai, client):
    resp = client.post('/api/ask', json={
        'story_id': 'hiccup',
        'question': '   ',
        'messages': [],
    })
    assert resp.status_code == 400


@patch('app.client')
def test_check_clues_returns_discovered_indices(mock_openai, client):
    mock_openai.chat.completions.create.return_value = MagicMock(
        choices=[MagicMock(message=MagicMock(content='[true, false, true]'))]
    )
    resp = client.post('/api/check-clues', json={
        'story_id': 'hiccup',
        'messages': [
            {'role': 'user', 'content': '男人是不是在打嗝？'},
            {'role': 'assistant', 'content': '是。'},
        ],
    })
    assert resp.status_code == 200
    assert resp.get_json()['discovered_clues'] == [0, 2]


def test_check_clues_story_not_found(client):
    resp = client.post('/api/check-clues', json={
        'story_id': 'not-exist',
        'messages': [],
    })
    assert resp.status_code == 404
```

- [ ] **Step 2: 运行测试**

Run: `pytest tests/test_api.py -v`
Expected: 全部 6 个测试通过（`test_ask_returns_normalized_answer` 和 `test_check_clues_returns_discovered_indices` 依赖 mock，不需要真实 API）。

## Task 10: 端到端验证

- [ ] **Step 1: 启动后端**

Run: `python app.py`
Expected: 服务监听 `http://127.0.0.1:5000`。

- [ ] **Step 2: 打开前端并走通完整流程**

Open: `http://127.0.0.1:5000`
Verify:
1. 首页显示“打嗝的男人”故事卡片
2. 点击卡片进入游戏页，汤面正确显示
3. 输入问题，AI 只回复“是 / 否 / 是也不是 / 无关”
4. 命中核心线索后，侦探笔记新增一条发现
5. 未命中时，侦探笔记不变，不显示未解锁线索
6. 三条核心线索都解锁后，“揭开汤底”按钮可用，点击显示汤底
7. 刷新页面后游戏重置

- [ ] **Step 3: 验证 API 密钥不泄露**

Open browser DevTools → Network → 检查 `/api/ask` 和 `/api/check-clues` 请求。
Verify: 请求体/响应中均不包含 `DEEPSEEK_API_KEY`。

- [ ] **Step 4: 验证移动端布局**

Use browser DevTools responsive mode (width < 768px).
Verify: 页面变为单列，输入框和线索板正常显示，无横向滚动。

## Self-Review Checklist

- **Spec coverage:**
  - [x] 故事列表：Task 3 `/api/stories` + Task 8 首页渲染
  - [x] 汤面展示：Task 6 HTML + Task 8 `renderGame`
  - [x] 提问与四类回答：Task 4 `/api/ask` + prompt
  - [x] 核心线索判定：Task 5 `/api/check-clues` + prompt
  - [x] 只显示已发现线索：Task 8 `renderClues`
  - [x] 解锁后显示汤底：Task 8 `revealAnswer`
  - [x] API 密钥保护：Task 1 `.env` + `.gitignore`
  - [x] 复古视觉风格：Task 7 CSS
  - [x] 可扩展故事格式：Task 2 JSON 文件

- **Placeholder scan:** 无 TBD/TODO/空泛描述。
- **Type consistency:** API 字段 `story_id`、`messages`、`discovered_clues` 在全端一致。

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-16-海龟汤网页-plan.md`.

Two execution options:

1. **Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
